<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <parent>
    <groupId>org.opendaylight.odlparent</groupId>
    <artifactId>odlparent</artifactId>
    <version>1.6.1-SNAPSHOT</version>
    <relativePath/>
  </parent>
  <modelVersion>4.0.0</modelVersion>
  <groupId>org.opendaylight.integration</groupId>
  <artifactId>root</artifactId>
  <version>0.4.1-SNAPSHOT</version>
  <name>integration</name> <!-- Used by Sonar to set project name -->
  <packaging>pom</packaging>
  <url>https://wiki.opendaylight.org/view/CrossProject:Integration_Group</url>
  <scm>
    <connection>scm:git:ssh://git.opendaylight.org:29418/integration.git</connection>
    <developerConnection>scm:git:ssh://git.opendaylight.org:29418/integration.git</developerConnection>
    <url>https://wiki.opendaylight.org/view/CrossProject:Integration_Group</url>
    <tag>HEAD</tag>
  </scm>
  <properties>
    <!-- Keep this list sorted alphabetically please -->
    <didm.version>0.2.1-SNAPSHOT</didm.version>
    <feature.alto.version>0.2.1-SNAPSHOT</feature.alto.version>
    <feature.benchmark.version>1.1.1-SNAPSHOT</feature.benchmark.version>
    <feature.bgpcep.version>0.5.1-SNAPSHOT</feature.bgpcep.version>
    <feature.capwap.version>0.2.1-SNAPSHOT</feature.capwap.version>
    <feature.centinel.version>0.1.1-SNAPSHOT</feature.centinel.version>
    <feature.controller.config.version>0.4.1-SNAPSHOT</feature.controller.config.version>
    <feature.controller.extras.version>1.6.1-SNAPSHOT</feature.controller.extras.version>
    <feature.dlux.version>0.3.1-SNAPSHOT</feature.dlux.version>
    <feature.faas.version>1.0.1-SNAPSHOT</feature.faas.version>
    <feature.flow.version>1.3.1-SNAPSHOT</feature.flow.version>
    <feature.groupbasedpolicy.version>0.3.1-SNAPSHOT</feature.groupbasedpolicy.version>
    <feature.iotdm.version>0.1.1-SNAPSHOT</feature.iotdm.version>
    <feature.lacp.version>1.1.1-SNAPSHOT</feature.lacp.version>
    <feature.lispflowmapping.version>1.3.1-SNAPSHOT</feature.lispflowmapping.version>
    <feature.mdsal.version>1.3.1-SNAPSHOT</feature.mdsal.version>
    <feature.messaging4transport.version>1.0.1-SNAPSHOT</feature.messaging4transport.version>
    <feature.nemo.version>1.0.1-SNAPSHOT</feature.nemo.version>
    <feature.netconf.version>1.0.1-SNAPSHOT</feature.netconf.version>
    <feature.netide.version>0.1.1-SNAPSHOT</feature.netide.version>
    <feature.nic.version>1.1.1-SNAPSHOT</feature.nic.version>
    <feature.neutron.version>0.6.1-SNAPSHOT</feature.neutron.version>
    <feature.of-config.version>1.0.1-SNAPSHOT</feature.of-config.version>
    <feature.openflowplugin.version>0.2.1-SNAPSHOT</feature.openflowplugin.version>
    <feature.ovsdb.version>1.2.2-SNAPSHOT</feature.ovsdb.version>
    <feature.packetcable.version>1.3.1-SNAPSHOT</feature.packetcable.version>
    <feature.restconf.version>1.3.1-SNAPSHOT</feature.restconf.version>
    <feature.sdninterfaceapp.version>1.6.1-SNAPSHOT</feature.sdninterfaceapp.version>
    <feature.sfc.version>0.2.1-SNAPSHOT</feature.sfc.version>
    <feature.snbi.version>1.2.1-SNAPSHOT</feature.snbi.version>
    <feature.snmp4sdn.version>0.3.1-SNAPSHOT</feature.snmp4sdn.version>
    <feature.sxp.version>1.2.1-SNAPSHOT</feature.sxp.version>
    <feature.tcpmd5.version>1.2.1-SNAPSHOT</feature.tcpmd5.version>
    <feature.topoprocessing.version>0.1.1-SNAPSHOT</feature.topoprocessing.version>
    <feature.tsdr.version>1.1.1-SNAPSHOT</feature.tsdr.version>
    <feature.ttp.version>0.2.1-SNAPSHOT</feature.ttp.version>
    <feature.unimgr.version>0.0.2-SNAPSHOT</feature.unimgr.version>
    <feature.usc.version>1.1.1-SNAPSHOT</feature.usc.version>
    <feature.usecplugin.version>1.0.1-SNAPSHOT</feature.usecplugin.version>
    <feature.vpnservice.version>0.2.1-SNAPSHOT</feature.vpnservice.version>
    <feature.vtn-manager.version>0.4.1-SNAPSHOT</feature.vtn-manager.version>
    <feature.yang-push.version>1.0.1-SNAPSHOT</feature.yang-push.version>
    <feature.yangtools.version>0.8.1-SNAPSHOT</feature.yangtools.version>
    <karaf.branding.version>1.2.1-SNAPSHOT</karaf.branding.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <snmp.plugin.version>1.1.1-SNAPSHOT</snmp.plugin.version>
    <!-- Keep this list sorted alphabetically please -->
  </properties>

  <modules>
    <module>distribution-karaf</module>
    <module>features-index</module>
    <module>features-test</module>
    <module>version</module>
  </modules>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-maven</id>
            <goals>
              <goal>enforce</goal>
            </goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>3.1.1</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
